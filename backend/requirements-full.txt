# MZQA Equity Research Terminal backend — Python dependencies
# Install into a local virtualenv:  python -m venv .venv && .venv\Scripts\pip install -r backend/requirements.txt
# This is the full superset: API + data layer + macro-cycle ML models + Dash ops dashboard.

# --- Web / API ---
fastapi>=0.115,<1.0
uvicorn[standard]>=0.32,<1.0
asyncpg>=0.30,<1.0
pydantic>=2.9,<3.0
pydantic-settings>=2.5,<3.0
python-dotenv>=1.0,<2.0
structlog>=24.4,<25.0
httpx>=0.27,<1.0

# --- LLM / graph orchestration ---
langchain-core>=0.3,<0.4
langchain-community>=0.3,<0.4
langgraph>=0.2,<0.3
langgraph-checkpoint-postgres>=2.0,<3.0

# --- Scheduling ---
apscheduler>=3.10,<4.0

# --- Auth / billing ---
bcrypt>=4.1
PyJWT>=2.8

# --- Data / numeric ---
pandas>=2.0
numpy>=1.26
scipy>=1.11
psycopg2-binary>=2.9
python-dateutil>=2.9
pytz>=2024.1

# --- Portfolio optimization ---
cvxpy>=1.4

# --- Macro-cycle models (xbrl_sec/sec/cycle: HMM + VAE regime detection) ---
torch>=2.2
transformers>=4.40
hmmlearn>=0.3

# --- Macro data sources ---
fredapi>=0.5

# --- Market data / scraping / parsing ---
yfinance>=0.2
curl_cffi>=0.6
requests>=2.31
selenium>=4.20
lxml>=5.0
beautifulsoup4>=4.12
trafilatura>=1.8
PyYAML>=6.0
tenacity>=8.2

# --- Ops dashboard (Dash / Plotly) — ops/dashboard ---
dash>=2.16
dash-bootstrap-components>=1.5
flask>=3.0
plotly>=5.20
